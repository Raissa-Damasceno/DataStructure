const firstArr = [0, 3, 4, 31];
const secondArr = [4, 6, 30];

function mergeSortedArrays(firstArr, secondArr) {
  const mergedArray = [];

  return mergedArray;
}

mergeSortedArrays(firstArr, secondArr);
